# 安全策略

## 支持范围

项目当前维护 `main` 分支的最新版本。安全修复可能不会回移到旧提交。

## 报告漏洞

请不要公开提交包含利用细节、真实 API Key 或用户数据的 Issue。请通过仓库的 GitHub Security Advisory 页面选择 **Report a vulnerability** 私下报告，并提供：

- 受影响的版本或提交；
- 可复现步骤与影响范围；
- 已知的缓解方式；
- 如适用，使用假凭据制作的最小复现。

维护者会尽快确认报告，并在修复可用后协调披露。

## 部署责任边界

本项目没有账号体系。公网或多人部署必须在反向代理层增加访问控制，并建议设置 `LLM_CONFIG_LOCKED=1`。模型请求会发送给部署者选择的模型供应商；只有 Ollama 等本机模型可以做到完全离线。
